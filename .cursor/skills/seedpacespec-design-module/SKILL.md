---
name: seedpacespec-design-module
id: seedpacespec-design-module
category: Design
description: >
  用自然语言描述一个复杂纯逻辑模块，直接输出独立架构设计文档——无需 PRD、无需视觉稿，产出物不合并到 TRD。
  TRIGGER when: （当前会话已使用过 seedpacespec/sdx 工作流，或用户消息明确提到 seedpacespec/sdx）且用户想设计非 UI 驱动的纯逻辑模块（动画控制器、播放引擎、同步系统、状态机、插件体系、调度器等）。
  SKIP: 用户描述的是业务需求或功能特性（如"加个登录功能"、"做个订单页面"）→ 建议走 sdx-propose 全流程；用户想单独生成 TRD → sdx-trd-generator。
  requires: 建议先有 architecture.md 提供全局约束。
  output: 独立的模块架构设计文档（组件分层、状态机、接口定义、技术决策），不合并到变更目录的 TRD 中。
  examples: "/sdx-design-module 动画控制器，支持并发和中断恢复" → 触发；"我想做个用户登录功能" → 不触发，建议 sdx-propose；"帮我设计个文件上传引擎" → 触发。
license: MIT
metadata:
  author: seedpacespec
  version: "1.0"
---

# seedpacespec-design-module

## 触发命令

```
/sdx-design-module <模块描述>
```

**示例**：
- `/sdx-design-module 动画控制器，多个 lottie 动画按顺序衔接，支持并发控制和中断恢复`
- `/sdx-design-module 文件上传引擎，支持分片、断点续传、加密`
- `/sdx-design-module 离线缓存同步系统`

参数为模块的自然语言描述，也可不带参数直接 `/sdx-design-module`，AI 会追问需求。

---

## 定位

独立于 TRD 生成流程，专门为**无 PRD、无视觉稿的复杂逻辑模块**输出设计方案。

**典型场景**：
- "我要做一个动画控制器，多个 lottie 动画衔接，需要并发控制"
- "帮我设计一个离线缓存同步引擎"
- "音频播放器需要支持插件扩展，帮我出个方案"
- "设计一个任务调度器，支持优先级队列和重试"

**不适用**：
- 有 PRD 的完整功能 → 用 `/sdx-trd-generator`
- 简单工具函数或单类封装 → 直接写代码，不需要设计方案

---

## 输入

用户用自然语言描述模块需求。没有固定格式，AI 从描述中提取以下信息：

| 要素 | 说明 | 示例 |
|------|------|------|
| **模块目标** | 这个模块要解决什么问题 | "控制多个 lottie 动画按顺序/并行播放" |
| **核心能力** | 模块要支持哪些关键操作 | "播放、暂停、跳转、插入、取消" |
| **变化点** | 哪些部分未来可能变化或需要扩展 | "动画类型可能新增 spine、视频片段" |
| **约束** | 性能、平台、兼容性等限制 | "Flutter 端，动画切换间隔 < 16ms" |
| **已有上下文** | 项目中已有的相关代码、架构约定 | "项目用 Bloc 管状态，有 architecture.md" |

---

## Workflow

### Step 1: 场景甄别 + 理解需求 + 加载上下文

**1a. 场景甄别（防误用，AskQuestion）**

> **两种调用方式**：
> - **用户直接调用** `/sdx-design-module` → 走下方 AskQuestion 甄别
> - **被其他 skill 内部引用**（trd-generator Step 7 / propose design 步骤遇到复杂模块时,自动按本 skill Step 3 的思考粒度展开）→ 不走本步甄别,直接使用 Step 3 的输出结构

用户直接调用时,先判断是否适合本 skill。用 AskQuestion 确认：

```
你描述的模块属于哪种情况？

A) 独立复杂模块 — 没有 PRD,是基于口头描述的独立系统/公共组件/引擎
   (如：动画控制器、离线缓存引擎、插件体系、状态机框架)
   → ✅ 适合本命令,继续

B) 某个需求的技术方案 — 有 PRD,是某个功能需求的实现方案
   → ❌ 应该用 /sdx-trd-generator 生成 TRD
   (如果你在 propose/trd-generator 流程中遇到了复杂模块,
    不需要手动调本命令——那些流程会自动按本 skill 的思考粒度展开)
```

- 选 **A** → 继续 Step 1b
- 选 **B** → 提示用户「建议使用 `/sdx-trd-generator`」,结束本 skill

**1b. 理解需求**

1. 从用户描述中提取上述要素，列出关键信息确认理解无误
2. 如果用户提供了项目路径或提到了 `architecture.md` → 读取，后续设计与项目架构保持一致
3. 如果用户提到了已有代码（"参考 XX 模块"）→ 读取，了解已有模式

### Step 2: 复杂度判定

对用户描述做以下扫描，逐条评估并输出判定表：

| # | 检查项 | 怎么判断 | 本次结果 |
|---|--------|---------|---------|
| 1 | **协作实体数** | 完成该模块需要的类/组件 ≥ 3 个，且有调用或数据传递关系 | ✅/❌ |
| 2 | **抽象层级** | 需要接口/抽象类 + 多个实现来屏蔽变化 | ✅/❌ |
| 3 | **异步编排深度** | 异步步骤 ≥ 3 步，且有顺序依赖、分支或失败回退 | ✅/❌ |
| 4 | **状态复杂度** | 状态 ≥ 3 个，转移条件非 trivial | ✅/❌ |
| 5 | **扩展点需求** | 描述中有"后续新增"、"可配"、"插件"等可扩展性要求 | ✅/❌ |
| 6 | **跨模块副作用** | 操作会触发其他模块的状态变更、事件、UI 刷新 | ✅/❌ |

**命中 ≥ 2 项** → 输出完整设计方案（Step 3 全量执行）

**命中 0-1 项** → 用 AskQuestion 告知用户："这个模块复杂度不高，建议直接写职责说明 + 接口定义，不需要完整架构设计。要继续完整方案还是简化版？"
- 用户选完整 → Step 3 全量
- 用户选简化 → 只输出 Step 3 中的第 3、5 节（职责表 + 接口签名）

### Step 3: 生成设计方案

读取设计写法标准：

```
Read: .cursor/skills/seedpacespec-trd-generator/references/frontend/ui-component-guide.md
→ 定位到 "### 复杂逻辑系统的写法" 章节，按其中的写法标准执行
```

按以下结构输出设计方案文档。每一节都必须用**同一个模块**的例子贯穿，不得在不同节之间切换例子。

---

#### 输出模板

```markdown
# {模块名} 设计方案

> **场景**: {一句话描述要解决的问题}
> **技术栈**: {语言/框架}
> **复杂度判定**: {命中 N/6 项}（{列出命中项}）
> **日期**: {date}

---

## 1. 类图（UML）

> 用 mermaid classDiagram 画出所有核心类/接口及其关系。
> 连线上标注设计模式名称（Strategy / Observer / Template Method 等）。

{mermaid classDiagram}

**设计模式速查**

| 模式 | 落位 | 一句话说明 |
|------|------|-----------|
| ... | ... | ... |

## 2. 模块图

> 用 mermaid graph 画出整体架构分层：内核、可替换层、可插拔扩展。
> 标注模块间的通信方式（接口调用 / 事件广播 / 数据流）。

{mermaid graph}

## 3. 职责表

| 模块 | 职责边界 | 关键接口 | 不能负责什么 |
|------|---------|---------|-------------|
| ... | ... | ... | ... |

## 4. 架构模式选型

| 设计点 | 设计模式 | 落位 | 为什么选 | 为什么不选更简单方案 |
|-------|---------|------|---------|-------------------|
| ... | ... | ... | ... | ... |

## 5. 关键伪代码

> 每个设计模式给出骨架伪代码，展示：
> - 抽象接口定义
> - 注入/注册方式
> - 核心编排逻辑
> - 异常隔离策略
>
> 伪代码后跟中文白话解读。

### {模式名} — {一句话说明}

{伪代码}

> **白话**：{解释注入点在哪、运行时怎么切换、异常怎么隔离}

## 6. 协作流程

> 选一个最能体现模块复杂度的完整场景，从入口到结束逐步写清：
> - 谁发起、谁接收
> - 中间经过哪些模块、事件、状态转移
> - 失败/异常时谁兜底、怎么恢复

**场景：{具体场景描述}**

> 1. ...
> 2. ...
> ...

## 7. 边界与异常

| 关注点 | 方案至少要写清什么 |
|-------|------------------|
| ... | ... |

## 8. 待确认项（可选）

- [ ] {需要和用户/团队确认的设计决策}
```

---

### Step 4: 保存

方案生成后，询问用户保存位置。提供默认选项：

1. **有变更目录** → 保存到 `seedpacespec/changes/active-change/<变更名>/designs/{模块名}-design.md`
2. **无变更目录** → 保存到用户指定路径，或当前工作目录下 `{模块名}-design.md`
3. **不保存** → 仅在对话中展示

---

## 写作标准

本 skill 的设计方案写作标准与 `seedpacespec-trd-generator` 中"复杂逻辑系统的写法"完全一致。

**核心要求**（摘自 `references/frontend/ui-component-guide.md`）：

1. **同一例子贯穿所有章节** — 类图、模块图、职责表、伪代码、协作流程用同一个模块
2. **设计模式必须有伪代码** — 不能只说"用策略模式"，要写到"注入点在哪、切换时怎么处理、异常怎么隔离"
3. **协作流程要有深度** — 不是"A 调 B 调 C"，而是写清每步的输入输出、失败回退、状态流转
4. **边界与异常** — 并发、资源释放、状态恢复、降级策略
5. **白话解读** — 伪代码块后必须跟中文白话，解释编排逻辑和兜底策略

**禁止**：
- 只有骨架没有深度（"AnimationManager — 管理动画"）
- 过度设计简单模块（一个 boolean 能解决的别上状态机）
- 类图没有模式标注
- 伪代码没有白话解读

---

## 与其他 skill 的关系

| 场景 | 用哪个 |
|------|--------|
| 有 PRD，需要完整 TRD | `seedpacespec-trd-generator` |
| 无 PRD，纯逻辑模块设计 | **本 skill** |
| 探索想法、排查问题 | `seedpacespec-explore` |
| 实现代码 | `seedpacespec-apply-change` |

**衔接**：本 skill 输出的设计方案可直接作为 `seedpacespec-apply-change` 的输入，跳过 TRD 流程直接实现。
