# [项目名称] - 架构概览

> 本文档由 `seedpacespec-analyzer-project` 自动生成，为 AI Agent 提供代码仓库的结构化全局上下文。
> 生成时间: [ISO 日期] | 基准提交: [commit hash]
> **分析范围**: [用户选定的 focus_paths，或"全仓库"]

> **增量更新记录**（最新在前）：
>
> - [日期] ([old_commit..new_commit]): 变更摘要

## 文档用途（下游消费指引）

> 本节为**导读**：说明各章在后序 Skill / 会话中的典型用法。**不改变**正文「只记录客观事实」的写作规则；事实陈述仍以 §0～§9 为准。

**为何维护本文档**：把仓库现状结构化固化，便于新 PRD 与历史交付对照、`design`/TRD 贴合现有分层、缺陷检测收窄范围、生成代码对齐既有约定。

| 文档章节 | 主要服务下游 | 用途说明 |
| -------- | ------------ | -------- |
| **§4 核心业务域** | 迭代类 / 优化类需求，`sdx-propose`、`sdx-guided`；与 PRD、`seedpacespec/global-specs/changelog.md` 联读 | 标明历史上**已落地的业务能力**落在哪些域、路径与边界。新需求来时做**语义对照**：是否与本章某域同一业务主题（例如本章已归纳「家长音视频通话」相关域与数据流，而新 PRD 仍是家长侧音视频），从而判断是**域内演进**还是新开模块，减少重复实现与挂错层级。**强调**：对照依赖本章事实写得足够可辨认（域名、路径、能力描述），而非仅靠章节标题。 |
| **§3 架构设计、§2 技术栈、分层映射** | `design.md`、TRD、技术方案评审 | 使方案中的分层命名、目录挂载、层间调用与依赖边界与**当前仓库真实结构**一致，避免设计与实现脱节。 |
| **§9 风险热点**（耦合、高频变更、交叉矩阵等） | `sdx-detect`、缺陷检测、改动风险评估 | 优先审查高耦合、大文件、高频变更与缺测试叠加处，缩小质检与回归范围。 |
| **§6 编码约定** | `sdx-apply`、代码生成与 Review | 对齐命名、状态管理、路由、国际化与 lint 等既有规范，使生成或修改的代码符合本项目习惯。 |

## 0. Agent 速览

**项目**: [项目名称]，[项目类型]，[面向场景/用户]
**项目形态**: application / sdk / hybrid
**技术栈关键词**: [语言] + [框架] + [状态管理/ORM] + [构建工具]
**开发启动**: [启动命令]
**核心入口文件**: [列表，含路径]
**关键约定**:

1. [最重要的路径/命名/结构约定]
2. [核心数据流的关键路径概述]
3. [容易出错的客观事实，如命名不一致的目录]

## 1. 项目概述

**[项目名称]** 是 [项目类型]，用于 [核心目标]。

### 核心能力（由路由/API/目录结构客观对应）

- **能力 1**：描述
- **能力 2**：描述

### 仓库规模

| 指标        | 数值 |
| ----------- | ---- |
| 源码文件数  | N    |
| 代码总行数  | ~N   |
| 子包/模块数 | N    |

### 多端/多包架构（如适用）

| 包/目录 | 用途 |
| ------- | ---- |

## 2. 技术栈

### 2.1 [端/层名称]（如：前端 / 服务端 / 核心库）

| 类别     | 技术 | 版本（声明） |
| -------- | ---- | ------------ |
| 语言/SDK | ...  | ...          |
| 框架     | ...  | ...          |
| 状态管理 | ...  | ...          |
| 路由     | ...  | ...          |
| 网络     | ...  | ...          |
| 存储/ORM | ...  | ...          |
| 构建     | ...  | ...          |
| 测试     | ...  | ...          |

（按项目实际端/层分多个子章节，每端一个表格）

### 2.N 技术栈事实性标注

以下为代码中客观可验证的异常或不一致：

- **声明未使用的依赖**: [在包管理文件中声明但源码中未检出引用的依赖列表]
- **同功能多实现**: [如"网络请求同时使用 axios（文件A）和 fetch（文件B）"]
- **版本不一致**: [如"A 包声明 ^3.0 但 lock 文件中实际为 2.x"]
- **命名不一致**: [如"目录名 contants 疑似拼写错误，项目中同时存在 constants 和 contants"]

### 2.N+1 构建与产物路径

（构建工具、产物输出路径、环境配置等跨多个文件才能拼出的关联信息）

### 2.N+2 开发脚本

（关键脚本及其作用）

### 2.N+3 SDK 契约概览（仅项目形态为 sdk 或 hybrid 时输出，application 省略本节）

**公开 API 清单**:

| 模块/命名空间 | 导出符号 | 类型（class/function/const/type） | 稳定性（stable/beta/deprecated） |
| ------------- | -------- | -------------------------------- | ------------------------------- |

**版本策略**:
- 当前版本：[x.y.z]
- semver 遵守情况（是否存在 breaking change 未升大版本的情况）
- 已废弃 API（标注 @deprecated 或等价标记的导出）

**包分发**:
- 产物格式（ESM/CJS/UMD/AAR/wheel/crate 等）
- 入口文件（main/module/exports 字段指向）
- 体积约束（若有 bundlesize/size-limit 等配置）
- peer dependencies（消费方需自行安装的依赖）

## 3. 架构设计

### 3.1 架构风格

（一段话描述整体架构风格）

### 3.2 分层架构

**分层映射**（下游 TRD 方案章节子章节名从此表读取）:

| 层名 | 层角色（一句话） | 对应目录 | 职责 |
| ---- | --------------- | -------- | ---- |
| {从项目归纳的层名1} | {该层做什么、不做什么} | `{实际目录}` | {具体职责} |
| {从项目归纳的层名2} | ... | ... | ... |
| ... | ... | ... | ... |

> 层名、层数完全由项目实际代码决定。不同项目的层可能完全不同——前端可能有 Model/Store/Hook/Page 四层，后端可能有 Controller/Service/DAO 三层，嵌入式可能有 HAL/Driver/Task 三层。禁止套用固定模板。

**层间调用规则**（从实际代码 import/调用关系中归纳）:

```
{层A} → {层B} → {层C} → {外部边界}
     ↓         ↓
{基础设施层}（被各层引用）
```

- [允许] {从代码中观察到的合理调用方向}
- [违反] {项目中实际存在的分层违反，标注具体文件}

**分层不一致记录**:
- {如：某文件混合了多层职责，标注文件名、行数、具体违反}

### 3.3 目录结构（节选）

（仅列出理解架构必需的关键目录，不做完整 tree 输出）

## 4. 核心业务域

### 4.N [域名称]（核心域）

**路径**: `...` **规模**: N 文件，~N 行

**内部结构**:
（域内各子目录/子模块的文件数和职责，使用项目实际目录命名）

**域内数据流路径**:
（从入口到出口的实际调用链，标注每一跳的文件名）

**域边界**:

- 对外暴露: [被其他域引用的导出]
- 外部依赖: [引用的其他域和公共模块]

**关键抽象**:
（该域中的核心抽象：类、接口、模块等）

**域内复杂度信号**:

- 最大单文件: [文件名, 行数]
- 最多分支逻辑的文件: [文件名]

#### 4.N.a 领域模型（可选：无材料则整节省略）

> **规则**：仅当该核心域内**客观存在**可列出的实体/类型模型/状态枚举时填写下表与图。**若不存在**：不要留空表或虚构实体；**删除本小节标题及表格**，或在「关键抽象」段落后用一句话说明「本域无显式领域模型层」。

**实体清单**:

| 实体 | 类型定义位置 | 关键字段 | 语义说明 |
| ---- | ------------ | -------- | -------- |
| [实体名] | `types/xxx.d.ts:12` | id, stages, status | [该实体是什么] |

**实体关系**:

```
[聚合根] 1──N [子实体] 1──N [值对象]
  │                        │
  └── [关联实体]            └── [联合类型: TypeA | TypeB | ...]
```

**状态枚举与流转**:

| 枚举/常量 | 定义位置 | 各状态值含义 | 流转条件 |
| --------- | -------- | ------------ | -------- |

#### 4.N.b 业务协同（可选：无编排/无实体参与则整节省略）

> **规则**：仅当存在可验证的多步业务流程与实体（或等价抽象）参与关系时撰写。**若不存在**：不写「核心场景」空壳；**删除本小节**。

**核心场景: [场景名]**

参与实体: [A, B, C]
编排机制: [Redux action 序列 / 事件 / 回调链]
编排代码: `[文件路径:行号]`

```
[触发条件] → Action1(Entity1) → Action2(Entity2, Entity3) → Action3(Entity1.update) → [结果]
```

| 实体 | 在本场景中的职责 |
| ---- | ---------------- |


### 4.M [域名称]（辅助域）

**路径**: `...` **规模**: N 文件，~N 行
**职责**: 一句话描述

## 5. 模块协作路径

### 5.1 端到端数据流路径

（贯穿多个域的完整调用链，标注每一跳经过的文件。域内数据流见 §4 各核心域章节。）
（**用途**：让 Agent 理解一个请求从用户操作到最终响应经过哪些模块，修改任意一跳时能判断上下游影响。）

## 6. 编码约定

- 命名规范（实际命名模式）
- 命名不一致记录
- 状态管理约定
- API 调用模式
- 路由约定
- 国际化方式
- lint 规则（关键的已启用/已禁用规则）

## 7. 配置管理

### 全局配置项

### 平台/环境差异分布

### 国际化

## 8. 测试与安全现状

### 测试现状

| 类型 | 位置 | 框架 | 覆盖程度 |
| ---- | ---- | ---- | -------- |

### 无测试保护的核心模块

### 安全现状

## 9. 风险热点

> 本节为下游 Skill 提供**优先分析清单**。每个子板块标注「用途」，说明该信号对后续 Agent 工作的指导意义。

### 9.1 大文件清单（>500 行）

**用途**：大文件修改时容易误改相邻逻辑，Agent 应优先拆分或限定修改范围。

| 文件 | 行数 | 所属域 |
| ---- | ---- | ------ |

### 9.2 耦合热点

**用途**：高被引用文件的签名/语义变更影响面不可控，Agent 修改前须确认所有引用方兼容。同时包含公共模块引用排名、全局状态消费方、跨域依赖清单。

#### 公共模块被引用 TOP 10

| 公共模块 | 被引用次数 | 引用方 |
| -------- | ---------- | ------ |

#### 全局状态依赖

| 状态对象 | 监听方/消费方 |
| -------- | ------------- |

#### 跨域依赖

| 源域 | 目标域 | 引用文件 |
| ---- | ------ | -------- |

### 9.3 平台/环境差异分支分布

**用途**：判断变更是否涉及多分支逻辑（如 dev/prod、iOS/Android），避免只改一个分支而遗漏另一个。

### 9.4 高频变更文件

**用途**：高频变更文件是多需求交汇处，修改时需注意与其他进行中的变更冲突。

| 文件 | 近 50 次提交修改次数 |
| ---- | -------------------- |

### 9.5 交叉风险矩阵

**用途**：多维信号交叉命中的文件是最高优先级的风险点，下游缺陷检测和代码审查应从这些文件开始。

| 文件 | >500行 | 近50次变更≥5 | 无测试 | 被引用≥5次 | 命中数 |
| ---- | :----: | :----------: | :----: | :--------: | :----: |

> 按命中数降序排列。仅列出命中 ≥2 项的文件。此表为客观信号交叉，供下游 Skill 作为优先分析清单。
